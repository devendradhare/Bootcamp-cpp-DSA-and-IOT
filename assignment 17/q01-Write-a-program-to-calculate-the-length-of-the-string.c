// 1.Write a program to calculate the length of the string. (without using built-inmethod)
#include <stdio.h>
int main()
{
    char s[50] = "hi, I am devendra dhare";
    int i;
    printf("\n\nstring = %s", s);

    for (i = 0; s[i]; i++)
    {
    }

    printf("\nlength of the string = %d\n\n",i);

    return 0;
}